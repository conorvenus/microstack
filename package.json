{
  "name": "microstack-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "runtime",
    "ui"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  }
}
